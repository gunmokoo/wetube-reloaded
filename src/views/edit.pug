extends base

block content 
    h4 Change Title of video
    form( method="POST") 
        input(name="title", placeholder="Video Tilte", value=video.title, required)
        input(value="Save", type="submit")